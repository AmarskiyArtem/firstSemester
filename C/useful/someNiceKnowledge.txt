#include<stdio.h> - библиотека ввода вывода
#include<stdbool.h> - подключение boolean
#include<locale.h> + в main '''setlocale(LC_ALL, "RU")'''; - подключение русского языка в консоли
#include<stdlib.h> + rand() % x (x - диапазон) - рандомные числа в си
scanf_s("%100s", String, (unsigned)sizeof(String)); - ввод строки
